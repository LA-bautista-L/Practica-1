/* practica 1.1
nombre:Luis Alfonso Bautista Lopez*/
 #include<stdio.h>
 #include<conio.h>

 int main (void){
 printf("1. suma\n");
 printf("2. resta\n");
 printf("3. multiplicacion\n");
 printf("4. division\n");
 printf("introduce 1 para suma, 2 para resta, 3 para multiplicacion, 4 para division");
 int opt;
 scanf("%d",&opt);
 int a;
 int b;
 printf("introduce la primera cantidad:");
 scanf("%d",&a);
 printf("introduce la segunda cantidad:");
 scanf("%d",&b);
 int c;
 if(opt==1){
 c =a+b;
 }
 else{
 c=a-b;
 }
 if(opt==3){
 c=a*b;
 }
 if(opt==4){
 c=a/b;
 }

 printf("el resultado es %d",c);
 }
